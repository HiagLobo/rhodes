# dependências e builds
node_modules/
dist/
coverage/
*.tsbuildinfo

# guarda: JS emitido por engano dentro de pasta fonte nunca entra no git
shared/src/*.js
shared/src/*.js.map

# dados — NUNCA no git (e nunca em pasta OneDrive; ver RHODES_DATA_DIR)
*.db
*.db-wal
*.db-shm
logs/
*.log

# segredos e ambiente
.env
.env.*

# binários de deploy (vivem em C:\rhodes\, nunca no repo)
deploy/*.exe


{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    // Tipos do @rhodes/shared direto da fonte (sem exigir build); em runtime o node
    // resolve via exports do pacote → shared/dist (integração registrada na Onda 01).
    "paths": {
      "@rhodes/shared": ["../shared/src/index.ts"]
    }
  },
  "include": ["src", "test", "drizzle.config.ts", "vitest.config.ts"]
}
